
import React from 'react'; // Reverted to standard React import
import Hero from '@/components/Hero'; // Import the Hero component

const Index: React.FC = () => { // Reverted to React.FC
  return (
    <>
      <Hero />
      {/* You can add other homepage sections here later if needed */}
    </>
  );
};

export default Index;
