# Core web framework
fastapi>=0.104.1
uvicorn>=0.24.0 # Server for FastAPI

# Environment variable management
python-dotenv>=1.0.0

# WebSocket support (though often an indirect FastAPI dependency)
websockets>=12.0

# Google Generative AI client
google-generativeai>=0.3.3

# Data validation
pydantic>=2.0.0

# Utilities
python-multipart>=0.0.5 

# Note: Starlette is a dependency of FastAPI.
# Note: Werkzeug was for Flask.
# Note: matplotlib, pydub (and its dependency ffmpeg) are considered dev dependencies (used by debug_utils.py).
# Note: All testing, build tools, and unused logging libraries have been removed.
#       These should be in a requirements-dev.txt or similar.
